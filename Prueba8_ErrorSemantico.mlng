# Prueba 8: Errores semanticos con sintaxis valida
int edad;
float promedio;
bool activo;
string nombre;

edad = 20;
promedio = 85.5;
activo = true;
nombre = "Andry";

edad = true;
promedio = nombre;
activo = 25;

x = 10;

int edad;

edad = promedio + activo;

if edad:
    Write(edad);

while nombre:
    edad = edad + 1;

def int suma(int a, int b):
    int resultado;
    resultado = a + b;
    return resultado;

suma(5);
suma(5, true);
resta(10, 5);

Write(variableNoDeclarada);
